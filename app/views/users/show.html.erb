<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= icon @user, size:80 %>
        <%= @user.name %>
        <%= "@#{@user.twitter_id}" %>
      </h1>
    </section>
  </aside>
</div>
